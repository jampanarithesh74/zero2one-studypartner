rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Project 3: Riddles Firebase Project Security Rules
    // Isolated to riddle sessions, participant submissions, and leaderboard

    match /events/{eventId}/activities/riddles {
      // Riddle Session Document (Admin controlled, public read)
      match /session/{docId} {
        allow read: if true;
        allow write: if true;
      }

      // Participant Riddle Submissions (Created upon solution check)
      match /submissions/{submissionId} {
        allow read: if true;
        allow create, update: if true;
        allow delete: if false;
      }

      // Riddle Activity Leaderboard (Read by all, written on score submit)
      match /leaderboard/{participantId} {
        allow read: if true;
        allow create, update: if true;
        allow delete: if false;
      }
    }

    match /{document=**} {
      allow read, write: if false;
    }
  }
}
